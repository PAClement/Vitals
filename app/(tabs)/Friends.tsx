import {ThemedText} from "@/components/ThemedText";
import RootView from "@/components/RootView";

export default function Friends() {
    return (
        <RootView>
            <ThemedText variant="headline">Friends</ThemedText>
        </RootView>
    );
}